деление X на 2 компилятор заменит на сдвиг байтов вправо на один.

деление X на 7 компилятор заменит на умножение на некоторую подобранную константу и сдвинет байты вправо на один.

деление X на Y компилятор не сможет оптимизировать, потому что Y станет известна только на этапе runtime. Код будет выполняться в разы дольше, чем оптимизированные варианты.